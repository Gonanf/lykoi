use super::types;

//TODO (Y repensar)
/*
pub struct node_data {
    line: u32,
    column: u32,
}

struct variable_data {
    name: Vec<u8>,
    data_type: types::primitives,
    node_data: node_data,
}
 */
